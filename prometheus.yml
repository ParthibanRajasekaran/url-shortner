global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: swiftlink
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - swiftlink-api:8080
